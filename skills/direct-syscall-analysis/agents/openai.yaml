display_name: "Direct syscall wrappers, inline assembly, libc bypass, register arguments, and runtime correlation"
short_description: "Beginner-friendly workflow for Direct syscall wrappers, inline assembly, libc bypass, register arguments, and runtime correlation. Extracts general methods and evidence practices without depending on any author, private repository, personal path, secret, or branded prompt."
default_prompt: "Use direct-syscall-analysis in beginner mode and produce an evidence-backed, reproducible result."
