# Example 08: Firmware filesystem analysis

Goal: identify architecture, filesystem, startup services, configuration, and exposed interfaces.

Route: `firmware-and-iot-analysis` -> `file-format-reversing` -> `native-binary-analysis`.

Success: the image hash, extraction command, service map, and binary relationships are reproducible.
