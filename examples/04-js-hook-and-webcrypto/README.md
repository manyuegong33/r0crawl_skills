# Example 04: JavaScript hook and WebCrypto boundary

Goal: capture plaintext and ciphertext boundaries without guessing the algorithm.

Route: `js-hook-engineering` -> `webcrypto-hooking` -> `crypto-dataflow-analysis`.

Success: algorithm, input bytes, key/IV/nonce handling, encoding, and call stack are recorded.
