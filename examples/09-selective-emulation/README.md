# Example 09: Selective function emulation

Goal: emulate one function with explicit memory, stubs, hooks, and fixtures.

Route: `selective-emulation-lab` -> `emulation-unicorn-angr` -> `reconstruction-and-parity`.

Success: the emulated result matches a runtime oracle for multiple inputs.
